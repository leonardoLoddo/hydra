interface:
  display_name: "Hydra"
  short_description: "Run parallel isolated work and scoped Hydra Arts"
  default_prompt: "Use $hydra to run the lightest safe parallel or isolated workflow, and select an Art only when its specific case applies."

policy:
  allow_implicit_invocation: true
